# Ralphy Progress Log

Mon Jan 19 09:33:49 PM -05 2026
Implemented cover image upload in document settings dialog
- Added email verification configuration to Better Auth in `packages/backend/convex/auth.ts`.
- Configured verification codes to be logged to the console in development mode.
- Enabled email verification requirement.

Mon Jan 19 2026
Add general navigation sidebar and reorganize header information into it
- Created `AppSidebar` component in `apps/studio/src/components/layout/app-sidebar.tsx`.
- Created `Layout` wrapper component in `apps/studio/src/components/layout/layout.tsx`.
- Wrapped `_auth` route with the new `Layout` component.
- Removed deprecated `PageHeader` component and its usages.
- Integrated `UserMenu` and `ThemeToggle` into the sidebar.

Review and improve breadcrumb implementation do something more like layout

Sun Feb 01 2026
Remove the "Save document" button on `/editor/new` (auto-save)
- Removed "Save Document" button and manual save handler from `apps/studio/src/routes/_auth/editor/new.tsx`.
- Implemented auto-save logic using `useDebouncedCallback` and `AdvancedEditor` callbacks.
- Added `onUpdate` to `AdvancedEditor` usage to ensure latest content is captured.
- Implemented `saveDocument` function with `useRef` to handle state freshness and prevent race conditions.
- Updated title change handler to trigger debounced save.

Sun Feb 01 2026
Fixed the admin review section:
- Implemented friendly URLs for admin review (/admin/review/$slug).
- Refactored review page structure to remove pending list and only show review actions + preview.
- Added search and status filtering (Pending, Published, All) to the admin dashboard.
- Implemented "Move to Pending" functionality for published documents.
- Added document context (cover image, curation spin) to the review sidebar.
- Updated backend queries and mutations to support these features.

Sun Feb 02 2026
Implemented production email sending with Resend for Better Auth:
- Added `resend` package (^4.0.0) to backend dependencies.
- Added `RESEND_API_KEY` and `EMAIL_FROM` environment variables to `packages/env/src/backend.ts`.
- Created `packages/backend/convex/email/` module with:
  - `client.ts` - Resend client initialization and helpers.
  - `templates/verification.ts` - HTML/text email verification templates with brand styling.
  - `templates/password-reset.ts` - HTML/text password reset templates with brand styling.
  - `send.ts` - Convex internal actions for sending emails via Resend.
  - `index.ts` - Module exports.
- Updated `packages/backend/convex/auth.ts` to integrate email sending:
  - Email verification sends real emails via Resend when API key is configured.
  - Password reset sends real emails via Resend when API key is configured.
  - Development mode logs verification/reset URLs to console for easy testing.
  - Graceful error handling (no app crashes if email fails).
